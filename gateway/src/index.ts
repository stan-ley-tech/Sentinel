// Sentinel gateway entrypoint. The reverse proxy, security pipeline, and
// config client land in later milestones — this is scaffolding only.
console.log("Sentinel gateway: scaffold only, not yet implemented.");
